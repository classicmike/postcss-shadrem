{
  "name": "postcss-shadrem",
  "version": "0.0.0",
  "description": "PostCSS plugin to replace rems with a custom shadrem unit in order to provide Rems like support within ShadowDOM using the font size of the shadow root as the main root for Rems like measurement",
  "keywords": ["postcss", "css", "postcss-plugin", "postcss-shadrem"],
  "scripts": {
    "unit": "node --test index.test.js",
    "test": "npm run unit && eslint ."
  },
  "author": "Michael Tran <michael.tran@sandstone.com.au>",
  "license": "MIT",
  "repository": "classicmike/postcss-shadrem",
  "engines": {
    "node": ">=18.0.0"
  },
  "peerDependencies": {
    "postcss": "^8.4.27"
  },
  "devDependencies": {
    "eslint": "^8.47.0",
    "postcss": "^8.4.27"
  },
  "eslintConfig": {
    "parserOptions": {
      "ecmaVersion": 2017
    },
    "env": {
      "node": true,
      "es6": true
    },
    "extends": ["eslint:recommended"]
  }
}
